var {Given,When, Then, } = require ('cucumber')

    Then(/^user validate the capture image$/, function (table) {
var data = table.hashes() [1];
      return console.log ("@Then -- user validates the capture image and country name: "+data['Country']+"  and the code is : " +data['Code'] );
    });
   